import 'package:flame/components.dart';

class Constants {
  static const double tileSize = 64;
  static const double gutter = 2;
  static const double gridSize = 31;
  static final worldSize = Vector2.all(tileSize * gridSize);
}
